<UserControl x:Class="ThermalAdiposer.Views.TopBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThermalAdiposer.Views"
             mc:Ignorable="d" 
             >
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Manage users" />
                <MenuItem Header="_Show groups" IsCheckable="True" IsChecked="True" />
            </MenuItem>
        </Menu>
        <Button DockPanel.Dock="Left" Content="_Save Image"/>
        <Button DockPanel.Dock="Left" Content="Select Tool"/>
        <Button DockPanel.Dock="Left" Content="Spotmeter Tool"/>
    </DockPanel>
</UserControl>
